// swift-tools-version:5.5

// swift-tools-version:5.5

import PackageDescription

let package = Package(
    name: "Koloda",
    products: [
        .library(
            name: "Koloda",
            targets: ["Koloda"]
        ),
    ],
    targets: [
        .target(
            name: "Koloda",
            dependencies: []),
    ]
)
